---
// 門面路由（非首頁）的安靜背景。沒有序列、沒有字標。
// 真正決定「這條路由該長什麼樣」的是 site-dither——它跨換頁只有一個擁有者，
// 這裡只負責讓那個擁有者在這條路由上被啟動。
---
<script>
  import { initSiteDither } from '../lib/site-dither';
  initSiteDither();
</script>
